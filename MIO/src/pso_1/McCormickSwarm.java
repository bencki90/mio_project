package pso_1;

import java.util.HashMap;

public class McCormickSwarm {//extends Swarm{

	/*public McCormickSwarm(int particlesNumber,
			HashMap<String, Dimension> dimensions, Operation operation,
			double C1, double C2) throws Exception {
		super(particlesNumber, dimensions, operation, C1, C2);
		// TODO Auto-generated constructor stub
	}

	@Override
	public double fitnessFunction(HashMap<String, Double> vars) {
		return  Math.sin(vars.get("x") + vars.get("y")) + Math.pow(vars.get("x") - vars.get("y"), 2) - 1.5 * vars.get("x") + 2.5 * vars.get("y") + 1;
	}
*/
}
